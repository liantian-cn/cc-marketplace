{
  "description": "SessionStart hook that warns when PKU_LAW_API is not configured.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PLUGIN_ROOT/hooks/check_env.js\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
